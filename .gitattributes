* text=auto

# to move existing files into LFS:
# git add --renormalize .
*.onnx filter=lfs diff=lfs merge=lfs -text
*.svg filter=lfs diff=lfs merge=lfs -text
*.png filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text
*.ttf filter=lfs diff=lfs merge=lfs -text
*.otf filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text

selfdrive/car/tests/test_models_segs.txt filter=lfs diff=lfs merge=lfs -text
system/hardware/tici/updater filter=lfs diff=lfs merge=lfs -text

# SIC-UEM: keep the small UEM menu icon as a normal git blob (NOT LFS), so it
# pushes to our GitHub fork without the sunnypilot GitLab LFS remote.
sunnypilot/selfdrive/assets/offroad/uem_*.png -filter -diff -merge -text

# ORBIT: keep the small ORBIT logo as a normal git blob (NOT LFS), same reason.
selfdrive/assets/img_orbit_logo.png -filter -diff -merge -text

# ORBIT: dragon credit logo ("powered by drago") on the home screen, non-LFS blob.
selfdrive/assets/img_drago_logo.png -filter -diff -merge -text

# ORBIT: README logos as normal git blobs (NOT LFS) so they render + push to the fork.
docs/images/*.png -filter -diff -merge -text
